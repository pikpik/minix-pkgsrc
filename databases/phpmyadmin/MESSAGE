$NetBSD$

Do *not* edit "${PREFIX}/share/phpmyadmin/config.inc.php" to customize
this package because it is a prototype file. Instead link, copy or map
the required files to your web space and create a local copy of
"config.inc.php" there.
