# $NetBSD$

MIMEDB_PATH="@MIMEDB_PATH@"
UPDATE_MIMEDB="@UPDATE_MIMEDB@"

case ${STAGE} in
POST-INSTALL)
	${UPDATE_MIMEDB} ${MIMEDB_PATH} >/dev/null
	;;
POST-DEINSTALL)
	${RM} -rf ${MIMEDB_PATH}
	;;
esac
