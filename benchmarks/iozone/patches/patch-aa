$NetBSD$

--- makefile.orig	Sun May  2 21:02:32 2004
+++ makefile
@@ -59,6 +59,7 @@ all:  
 	@echo "        ->   UWIN                 (32bit)   <-"
 	@echo "        ->   Windows (95/98/NT)   (32bit)   <-"
 	@echo ""
+	@false
 
 clean:
 	rm -f *.o iozone
