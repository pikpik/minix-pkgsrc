$NetBSD$

--- makefile.orig	2004-10-29 15:11:36.000000000 +0000
+++ makefile
@@ -67,6 +67,7 @@ all:  
 	@echo "        ->   UWIN                 (32bit)   <-"
 	@echo "        ->   Windows (95/98/NT)   (32bit)   <-"
 	@echo ""
+	@false
 
 clean:
 	rm -f *.o iozone
