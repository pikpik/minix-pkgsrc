$NetBSD$

--- makefile.orig	2005-09-13 17:26:54.000000000 +0000
+++ makefile
@@ -69,6 +69,7 @@ all:  
 	@echo "        ->   UWIN                 (32bit)   <-"
 	@echo "        ->   Windows (95/98/NT)   (32bit)   <-"
 	@echo ""
+	@false
 
 clean:
 	rm -f *.o iozone fileop
