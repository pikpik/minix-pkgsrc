$NetBSD$

--- replace.c.orig	Fri Jul  9 13:45:05 2010
+++ replace.c
@@ -101,6 +101,10 @@
 #ifndef HAVE_MKTIME
 /* mktime.c defines main() if DEBUG is set */
 #undef DEBUG
+/* HAVE_MKTIME is not defined, if mktime is present but broken,
+ * this causes linker errors
+ */
+#define mktime gawk_mktime
 #include "missing_d/mktime.c"
 #endif /* HAVE_MKTIME */
 
