===========================================================================
$NetBSD$

The default port number for the SKK service should be added to the
/etc/services and rebuild database:

    skkserv	1178/tcp

===========================================================================
