#! /bin/sh
#	$NetBSD: INSTALL,v 1.1 2002/05/31 13:00:02 seb Exp $

case "$2" in
POST-INSTALL)
	# install skkserv port number
	if grep '^skkserv[ 	]' /etc/services >/dev/null; then
		:
	else
		echo '
skkserv		1178/tcp		# SKK server' >>/etc/services
	fi
	;;
esac
