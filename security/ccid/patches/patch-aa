$NetBSD$

--- configure.in.orig	2008-01-22 12:33:34.000000000 -0700
+++ configure.in
@@ -93,6 +93,10 @@ case "$host" in
 	BUNDLE_HOST="FreeBSD"
 	DYN_LIB_EXT="so"
 	;;
+*-*-netbsd*)
+	BUNDLE_HOST="NetBSD"
+	DYN_LIB_EXT="so"
+	;;
 *-*-openbsd*)
 	BUNDLE_HOST="OpenBSD"
 	DYN_LIB_EXT="so.0.0"
