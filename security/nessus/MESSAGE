===========================================================================
$NetBSD$

The local state directory is now explictily defined as ${VARBASE}/nessus
for nessus components. 

===========================================================================
