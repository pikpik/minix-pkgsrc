# $NetBSD$

case ${STAGE} in
POST-INSTALL)
	#
	# Create the default search location for SASL authentication
	# mechanism libraries.
	#
	${RM} -f /usr/lib/sasl
	${LN} -s ${PKG_PREFIX}/lib/sasl /usr/lib/sasl
	;;
esac
