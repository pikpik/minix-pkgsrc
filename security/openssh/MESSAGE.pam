==========================================================================
$NetBSD$

To authenticate for SSH using PAM, add the contents of the file:

	${EGDIR}/sshd.pam

to your PAM configuration file.
==========================================================================
