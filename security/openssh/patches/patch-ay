$NetBSD$

--- openbsd-compat/getrrsetbyname.c.orig	Mon Jul 13 01:38:23 2009
+++ openbsd-compat/getrrsetbyname.c
@@ -108,6 +108,12 @@ struct __res_state _res;
 	(cp) += INT32SZ; \
 }
 #endif
+#ifdef __minix
+#define qdcount dh_qdcount
+#define ancount dh_ancount
+#define nscount dh_nscount
+#define arcount dh_arcount
+#endif
 
 /*
  * Routines to insert/extract short/long's.
