#!/bin/sh
#
# $NetBSD$
#
# PROVIDE: freewnn
# REQUIRE: DAEMON

. /etc/rc.subr

name="freewnn"
command_start="@PREFIX@/sbin/jserver"
command_stop="@PREFIX@/sbin/wnnkill"
pidfile=""

cmd=${1:-start}

case ${cmd} in
start)
    if [ -x ${command_start} ]; then
	echo "Start