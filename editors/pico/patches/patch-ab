$NetBSD$

--- pico/osdep/unix.orig	Fri Sep 15 01:45:19 2000
+++ pico/osdep/unix
@@ -2300,7 +2300,7 @@
 errstr(err)
 int err;
 {
-    return((err >= 0 && err < sys_nerr) ? (char *)sys_errlist[err] : NULL);
+    return strerror(err);
 }
 
 
