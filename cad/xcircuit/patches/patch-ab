$NetBSD$

--- configure.orig
+++ configure
@@ -7989,9 +7989,9 @@
 
 
       if test "$test_ok" = yes; then
-        SHLIB_CFLAGS="-fpic"
-        SHLIB_LD="ld"
-        LDDL_FLAGS="-Bshareable -x ${LIB_SPEC}"
+        SHLIB_CFLAGS="-fPIC"
+        SHLIB_LD=$CC
+        LDDL_FLAGS="-shared"
       fi
       ;;
 
