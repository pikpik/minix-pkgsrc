#!/bin/sh
#
# $NetBSD$
#

PKGNAME=$1
STAGE=$2

case ${STAGE} in
DEINSTALL)
	;;

POST-DEINSTALL)
	scrollkeeper-update -p ${LOCALBASE}/var/scrollkeeper
	;;
*)
	echo "Unexpected argument: ${STAGE}"
	exit 1
	;;
esac

exit 0
