#!/bin/sh
#
# $NetBSD$
#

USER="@GALE_USER@"
GROUP="@GALE_GROUP@"
USERDEL="@USERDEL@"
GROUPDEL="@GROUPDEL@"

case "$2" in
	DEINSTALL)
		echo "Removing user ${USER}"
		${USERDEL} -r ${USER}
		echo "Removing group ${GROUP}"
		${GROUPDEL} ${GROUP}
	;;
esac
exit 0
