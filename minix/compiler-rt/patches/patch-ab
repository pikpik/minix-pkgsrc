$NetBSD$

--- lib/divdc3.c.orig	Wed Jan 19 19:07:03 2011
+++ lib/divdc3.c
@@ -16,6 +16,8 @@
 #include <math.h>
 #include <complex.h>
 
+int fmax(int, int);
+
 /* Returns: the quotient of (a + ib) / (c + id) */
 
 double _Complex
