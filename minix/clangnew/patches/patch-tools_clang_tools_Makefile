$NetBSD$

--- tools/clang/tools/Makefile.orig	Tue Nov 29 00:14:27 2011
+++ tools/clang/tools/Makefile
@@ -12,8 +12,4 @@ DIRS := driver libclang c-index-test arcmt-test c-arcm
 
 include $(CLANG_LEVEL)/../../Makefile.config
 
-ifeq ($(OS), $(filter $(OS), Minix))
-DIRS := $(filter-out libclang c-index-test, arcmt-test, $(DIRS))
-endif
-
 include $(CLANG_LEVEL)/Makefile
