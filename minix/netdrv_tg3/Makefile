DISTNAME=		netdrv_tg3-1.0
CATEGORIES=		minix
MASTER_SITES=		# empty
DISTFILES=		# empty

MAINTAINER=		ben@minix3.org
HOMEPAGE=		http://www.minix3.org/
COMMENT=		Minix3 TG3 network driver
LICENSE=		gnu-gpl-v2

do-extract:
	@${CP} -R ${FILESDIR} ${WRKSRC}

.include "../../mk/bsd.prefs.mk"
.include "../../mk/bsd.pkg.mk"
