$NetBSD$

--- tar/util.c.orig	Sat Jun 26 13:59:07 2010
+++ tar/util.c
@@ -52,7 +52,7 @@ __FBSDID("$FreeBSD: src/usr.bin/tar/util.c,v 1.23 2008
 #ifdef HAVE_STRING_H
 #include <string.h>
 #endif
-#ifdef HAVE_WCTYPE_H
+#if defined(HAVE_WCTYPE_H) && !defined(__minix)
 #include <wctype.h>
 #else
 /* If we don't have wctype, we need to hack up some version of iswprint(). */
