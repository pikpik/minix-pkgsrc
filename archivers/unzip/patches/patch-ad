$NetBSD$

--- ttyio.c.orig	2008-01-05 20:24:16.000000000 +0000
+++ ttyio.c
@@ -54,6 +54,11 @@
 #  define HAVE_TERMIOS_H
 #endif
 
+#ifdef __minix
+/* For _POSIX_VERSION */
+#include <unistd.h>
+#endif
+
 #ifdef _POSIX_VERSION
 #  ifndef USE_POSIX_TERMIOS
 #    define USE_POSIX_TERMIOS  /* use POSIX style termio (termios) */
