$NetBSD$

--- unix/unix.c.orig	2010-07-18 01:43:48.000000000 +0000
+++ unix/unix.c
@@ -46,6 +46,14 @@
 #  endif
 #endif
 
+#ifdef __minix
+/* For _POSIX_VERSION */
+#include <unistd.h>
+
+#define lchown chown
+#define lchmod chmod
+#endif
+
 #ifdef _POSIX_VERSION
 #  ifndef DIRENT
 #    define DIRENT
