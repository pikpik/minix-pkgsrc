$NetBSD$

--- unix/unix.c.orig	Sat Mar  2 20:12:55 2002
+++ unix/unix.c
@@ -698,7 +698,11 @@
 #ifdef __QNXNTO__
       " (QNX Neutrino)",
 #else
+#ifdef __APPLE__
+      " (Mac OS X)",
+#else
       "",
+#endif /* Mac OS X */
 #endif /* QNX Neutrino */
 #endif /* QNX 4 */
 #endif /* Convex */
