$NetBSD$

--- zip.c.orig	Sat Mar  2 19:38:04 2002
+++ zip.c
@@ -248,7 +248,7 @@
     if (PERR(c))
       perror("zip I/O error");
     fflush(mesg);
-    fprintf(stderr, "\nzip error: %s (%s)\n", errors[c-1], h);
+    fprintf(stderr, "\nzip error: %s (%s)\n", ziperrors[c-1], h);
   }
   if (tempzip != NULL)
   {
