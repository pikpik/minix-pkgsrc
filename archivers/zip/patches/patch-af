$NetBSD$

--- zipnote.c.orig	Sat Mar  2 19:44:04 2002
+++ zipnote.c
@@ -59,7 +59,7 @@
 {
   if (PERR(c))
     perror("zipnote error");
-  fprintf(stderr, "zipnote error: %s (%s)\n", errors[c-1], h);
+  fprintf(stderr, "zipnote error: %s (%s)\n", ziperrors[c-1], h);
   if (tempzf != NULL)
     fclose(tempzf);
   if (tempzip != NULL)
