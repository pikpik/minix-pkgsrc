# $NetBSD$

case "${STAGE}" in
POST-DEINSTALL)
	${ECHO} ===========================================================================
	${ECHO}
	${ECHO} You may want to remove qmail-users package now, as it is no longer needed.
	${ECHO}
	${ECHO} ===========================================================================
	;;
esac
