# $NetBSD$

case "${STAGE}" in
POST-DEINSTALL)
	${ECHO} ===========================================================================
	${ECHO}
	${ECHO} Remember to remove popa3d from /etc/inetd.conf and restart inetd.
	${ECHO}
	${ECHO} ===========================================================================
	;;
esac
