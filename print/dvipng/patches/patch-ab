$NetBSD$

--- font.c.orig	Sun Nov 30 12:56:55 2003
+++ font.c
@@ -294,7 +294,6 @@ void ClearFonts(void)
   while(hfontptr!=NULL) {
     tmp=hfontptr->next;
     DoneFont(hfontptr);
-    free(hfontptr);
     hfontptr=tmp;
   }
   FreeFontNumP(dvi->fontnump);
