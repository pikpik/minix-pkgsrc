===========================================================================
$NetBSD$

Enable "module-native-protocol-tcp" in pulseaudio config (e.g. default.pa)
if there's no sound.

===========================================================================
