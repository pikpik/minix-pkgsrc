$NetBSD$

--- tools.h.orig	Sat Jan 23 16:10:54 2010
+++ tools.h
@@ -70,12 +70,16 @@
 #include <sys/utsname.h>
 #endif
 
-#if WANT_NBCOMPAT_H /* needed for strsep on SunOS < 5.11 */
+#if HAVE_NBCOMPAT_H /* needed for strsep on SunOS < 5.11 */
 #include <nbcompat.h>
 #include <nbcompat/string.h>
 #endif
 
+#ifdef LINE_MAX
 #define MAXLEN LINE_MAX
+#else
+#define MAXLEN 1024
+#endif
 #define MIDLEN 256
 #define SMLLEN 32
 #define D_WARN 5
