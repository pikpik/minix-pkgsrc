$NetBSD$

--- download.c.orig	Sat Jan 23 16:10:54 2010
+++ download.c
@@ -82,8 +82,10 @@ download_file(char *url, time_t *db_mtime)
 	else
 		p = (char *)url; /* should not happen */
 
+#ifndef __minix
 	if (st.size > SSIZE_MAX - 1)
 		err(EXIT_FAILURE, "file is too large");
+#endif
 
 	buf_len = st.size;
 	XMALLOC(file, sizeof(Dlfile));
