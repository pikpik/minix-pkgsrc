#!/bin/sh
#
# $NetBSD$

PKGNAME="$1"
STAGE="$2"

PKG_DBDIR="@PKG_DBDIR@"
MKDIR="@MKDIR@"

case ${STAGE} in
PRE-INSTALL)
	[ -d ${PKG_DBDIR} ] || ${MKDIR} ${PKG_DBDIR}
	;;

POST-INSTALL)
	[ -f ${PKG_DBDIR}/pkgdb.byfile.db ] || ${PKG_PREFIX}/bin/pkg_admin rebuild
	;;
esac
