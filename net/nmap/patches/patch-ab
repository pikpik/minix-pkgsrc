$NetBSD$

--- configure.orig	Sat Jul 20 09:48:46 2002
+++ configure
@@ -3450,6 +3450,7 @@
 extern char pcap_version[];
 int main() {
   int major, minor;
+  exit(0);	/*fool it*/
   sscanf(pcap_version,"%d.%d", &major, &minor);
   if (major > 0)
     exit(0);
