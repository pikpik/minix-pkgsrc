===========================================================================
$NetBSD$

	NOTE: ${PREFIX}/sbin/hping (and hping2) are setuid "root".

===========================================================================
