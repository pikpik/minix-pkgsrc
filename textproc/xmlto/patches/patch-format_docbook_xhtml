$NetBSD$

--- format/docbook/xhtml.orig	Thu Mar  3 15:19:13 2011
+++ format/docbook/xhtml
@@ -7,6 +7,6 @@ stylesheet)
   echo "http://docbook.sourceforge.net/release/xsl/current/xhtml/chunk.xsl"
   ;;
 post-process)
-  ${GCP_PATH:-cp} -R -P -p -- *.*htm* "$OUTPUT_DIR"
+  ${GCP_PATH:-cp} -R -p -- *.*htm* "$OUTPUT_DIR"
   ;;
 esac
