$NetBSD$

--- format/docbook/man.orig	Fri Sep 18 10:02:48 2009
+++ format/docbook/man
@@ -8,6 +8,6 @@ stylesheet)
   ;;
 post-process)
   [ -e "$XSLT_PROCESSED" ] && rm "$XSLT_PROCESSED"
-  ${GCP_PATH:-cp} -R -P -p -- * "$OUTPUT_DIR" 2>/dev/null
+  ${GCP_PATH:-cp} -R -p -- * "$OUTPUT_DIR" 2>/dev/null
   ;;
 esac
