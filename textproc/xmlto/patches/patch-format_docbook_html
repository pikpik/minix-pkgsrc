$NetBSD$

--- format/docbook/html.orig	Thu Mar  3 15:19:13 2011
+++ format/docbook/html
@@ -7,6 +7,6 @@ stylesheet)
   echo "http://docbook.sourceforge.net/release/xsl/current/html/chunk.xsl"
   ;;
 post-process)
-  ${GCP_PATH:-cp} -R -P -p -- * "$OUTPUT_DIR"
+  ${GCP_PATH:-cp} -R -p -- * "$OUTPUT_DIR"
   ;;
 esac
