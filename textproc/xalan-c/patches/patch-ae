$NetBSD$

--- configure.in.orig	Fri Oct  6 19:16:43 2000
+++ configure.in
@@ -40,6 +40,7 @@
                             *)            osver=HPUX11;;
                         esac;;
         *-*-mvs*)       platform=OS390 ;;
+        *-*-netbsd*)    platform=NETBSD ;;
         *)              platform=UNKNOWN ;;
 esac
 AC_SUBST(platform)
