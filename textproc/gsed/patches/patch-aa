$NetBSD$

--- lib/wchar.in.h.orig	2010-07-18 00:18:38.000000000 +0000
+++ lib/wchar.in.h
@@ -181,6 +181,10 @@ extern size_t mbrtowc (wchar_t *pwc, con
 #  define mbrlen rpl_mbrlen
 # endif
 # if !@HAVE_MBRLEN@ || @REPLACE_MBRLEN@
+#ifdef __minix
+/* Locale on Minix is broken */
+#define mbrlen my_mbrlen
+#endif
 extern size_t mbrlen (const char *s, size_t n, mbstate_t *ps);
 # endif
 #elif defined GNULIB_POSIXCHECK
