--- lib/util/include/girstring.h.orig	Fri Mar 25 16:26:48 2011
+++ lib/util/include/girstring.h	Thu Jun  2 12:07:32 2011
@@ -1,6 +1,24 @@
 #ifndef GIRSTRING_H_INCLUDED
 #define GIRSTRING_H_INCLUDED
 
+/* need this here for strdup() */
+#ifdef __minix
+
+/*
+ * This package doesn't respect pkgsrc's global CFLAGS, so we
+ * add the equivalent of -D_MINIX -D_POSIX_SOURCE here.
+ */
+
+#ifndef _MINIX
+#define _MINIX
+#endif
+
+#ifndef _POSIX_SOURCE
+#define _POSIX_SOURCE
+#endif
+
+#endif
+
 #include <string.h>
 
 #include "xmlrpc_config.h"
