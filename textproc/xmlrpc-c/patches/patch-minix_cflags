--- xmlrpc_config.h.in.orig	Thu Jun  2 11:51:03 2011
+++ xmlrpc_config.h.in	Thu Jun  2 11:54:41 2011
@@ -26,6 +26,23 @@
 #ifndef XMLRPC_CONFIG_H_INCLUDED
 #define XMLRPC_CONFIG_H_INCLUDED
 
+#ifdef __minix
+
+/*
+ * This package doesn't respect pkgsrc's global CFLAGS, so we
+ * add the equivalent of -D_MINIX -D_POSIX_SOURCE here.
+ */
+
+#ifndef _MINIX
+#define _MINIX
+#endif
+
+#ifndef _POSIX_SOURCE
+#define _POSIX_SOURCE
+#endif
+
+#endif
+
 /* We hope to replace xmlrpc_amconfig.h some day with something that 
    doesn't require a whole special set of software to build, to make
    Xmlrpc-c approachable by dumber developers.
