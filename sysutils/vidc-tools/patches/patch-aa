$NetBSD$

--- blanktime/Makefile.orig	Tue Oct 17 18:44:27 2000
+++ blanktime/Makefile
@@ -1,6 +1,6 @@
 PROG=blanktime
 SRCS=blanktime.c
-BINDIR=/usr/local/bin
-MANDIR=/usr/local/man
+BINDIR=${PREFIX}/bin
+MANDIR=${PREFIX}/man
 
 .include <bsd.prog.mk>
