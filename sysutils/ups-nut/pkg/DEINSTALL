#!/bin/sh
#
# $NetBSD$
#

USER="@NUT_USER@"
GROUP="@NUT_GROUP@"
USERDEL="@USERDEL@"
GROUPDEL="@GROUPDEL@"

case "$2" in
	DEINSTALL)
		echo "Removing user ${USER}"
		${USERDEL} -r ${USER}
		echo "Removing group ${GROUP}"
		${GROUPDEL} ${GROUP}
	;;
esac
exit 0
