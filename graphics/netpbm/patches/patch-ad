$NetBSD$

--- urt/rle_open_f.c.orig	2005-05-24 09:41:44.000000000 +0000
+++ urt/rle_open_f.c
@@ -35,7 +35,6 @@ my_popen(const char * const cmd, 
     int thepid = 0;
     int pipefd[2];
     int i;
-    extern int errno;
 
     /* Check args. */
     if ( *mode != 'r' && *mode != 'w' )
