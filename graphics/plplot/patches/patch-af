$NetBSD$

--- utils/pltek.c.orig
+++ utils/pltek.c
@@ -110,7 +110,7 @@
 	oldpage = ipage;
 	printf("Page %d/%d> ", ipage, npage);
 
-	gets(ibuf);
+	fgets(ibuf, sizeof(ibuf), stdin);
 	c = ibuf[0];
 
 /* User input a page number or a return */
