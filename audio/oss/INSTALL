# $NetBSD$

OSSLIBDIR="@OSSLIBDIR@"

case ${STAGE} in
POST-INSTALL)
	#
	# Create the OSS devices in /dev.
	#
	${ECHO}
	${ECHO} "Creating OSS devices..."
	cd /dev && ${SH} ${OSSLIBDIR}/MAKEDEV.oss all
	;;
esac
