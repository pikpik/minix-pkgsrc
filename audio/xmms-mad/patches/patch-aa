$NetBSD$

--- src/decoder.c.orig	Fri May 10 00:53:45 2002
+++ src/decoder.c
@@ -205,6 +205,9 @@ decode (void *arg)
   /* main loop */
   do
     {
+#ifdef _PTH_PTHREAD_H_
+	  sched_yield();
+#endif
       if (info->stop)
         break;
       if (seek_skip)
