$NetBSD$

--- sub.c.orig	Tue Oct  3 01:52:27 2000
+++ sub.c
@@ -38,6 +38,11 @@
   }
   if (dsp != NULL)
     XCloseDisplay(dsp);
+
+  /* kill outstanding subprocesses ie: external audio players */
+  signal(15, NULL);
+  killpg(0, 15);
+
   exit(code);
 }
 
