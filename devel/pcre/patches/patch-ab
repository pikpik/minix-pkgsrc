$NetBSD$

--- pcre-config.in.orig	2004-09-13 16:20:00.000000000 +0200
+++ pcre-config.in
@@ -17,6 +17,9 @@ case `uname -s` in
   *SunOS*)
   libR=" -R@libdir@"
   ;;
+  *BSD*)
+  libR=" -Wl,-R@libdir@"
+  ;;
 esac
 
 while test $# -gt 0; do
