$NetBSD$

--- src/run.c.orig	Wed Jan 24 04:35:10 2001
+++ src/run.c
@@ -189,6 +189,13 @@
 #endif
     if (pid == 0)
     {
+#ifdef SETXID_SUPPORT
+	if (flags & RUN_UNSETXID) {
+	    (void) setgid (getgid ());
+	    (void) setuid (getuid ());
+	}
+#endif
+
 	if (shin != 0)
 	{
 	    (void) dup2 (shin, 0);
