$NetBSD$

--- src/tag.c.orig	Wed Jun 14 21:32:51 2000
+++ src/tag.c
@@ -868,7 +868,7 @@
 	/* The tags is valid but not mentioned in val-tags.  Add it.  */
 	datum value;
 
-	if (noexec)
+	if (nolock)
 	{
 	    if (db != NULL)
 		dbm_close (db);
