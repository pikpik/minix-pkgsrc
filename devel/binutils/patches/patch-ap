$NetBSD$

--- include/aout/aout64.h.orig	Fri Jul  9 19:34:36 2010
+++ include/aout/aout64.h
@@ -275,15 +275,25 @@ struct internal_nlist
   bfd_vma n_value;			/* Value of symbol.  */
 };
 
-/* The n_type field is the symbol type, containing:  */
-
+/* The n_type field is the symbol type, containing the following
+ * These have to be undef'd as they conflict with similar definitions
+ * in aout.h on minix
+ */
+#undef N_UNDF	
 #define N_UNDF	0	/* Undefined symbol.  */
+#undef N_ABS 	
 #define N_ABS 	2	/* Absolute symbol -- defined at particular addr.  */
+#undef N_TEXT 	
 #define N_TEXT 	4	/* Text sym -- defined at offset in text seg.  */
+#undef N_DATA 	
 #define N_DATA 	6	/* Data sym -- defined at offset in data seg.  */
+#undef N_BSS 	
 #define N_BSS 	8	/* BSS  sym -- defined at offset in zero'd seg.  */
+#undef N_COMM	
 #define	N_COMM	0x12	/* Common symbol (visible after shared lib dynlink).  */
+#undef N_FN	
 #define N_FN	0x1f	/* File name of .o file.  */
+#undef N_FN_SEQ 
 #define	N_FN_SEQ 0x0C	/* N_FN from Sequent compilers (sigh).  */
 /* Note: N_EXT can only be usefully OR-ed with N_UNDF, N_ABS, N_TEXT,
    N_DATA, or N_BSS.  When the low-order bit of other types is set,
