$NetBSD$

--- daemon.c.orig	Wed Jun  1 22:36:22 2011
+++ daemon.c
@@ -920,7 +920,9 @@ static int service_loop(struct socketlist *socklist)
 					switch (errno) {
 					case EAGAIN:
 					case EINTR:
+#ifdef ECONNABORTED
 					case ECONNABORTED:
+#endif
 						continue;
 					default:
 						die_errno("accept returned");
