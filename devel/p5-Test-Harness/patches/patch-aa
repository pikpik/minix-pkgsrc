$NetBSD$

--- Makefile.PL.orig	Tue Jan  8 17:03:18 2002
+++ Makefile.PL
@@ -35,7 +35,6 @@
     NAME            => $PACKAGE,
     VERSION_FROM    => "lib/$PACKAGE_FILE.pm", # finds $VERSION
     PREREQ_PM       => { },
-    INSTALLDIRS     => 'perl',
 );
 
 
