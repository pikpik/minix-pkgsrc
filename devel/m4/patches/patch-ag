$NetBSD$

--- lib/spawni.c.orig	Wed Jul 21 00:13:45 2010
+++ lib/spawni.c
@@ -224,8 +224,7 @@ __spawni (pid_t *pid, const char *file,
 #endif
 
   /* Set the process group ID.  */
-  if ((flags & POSIX_SPAWN_SETPGROUP) != 0
-      && setpgid (0, attrp->_pgrp) != 0)
+  if ((flags & POSIX_SPAWN_SETPGROUP) != 0)
     _exit (SPAWN_ERROR);
 
   /* Set the effective user and group IDs.  */
