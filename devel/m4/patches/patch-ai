$NetBSD$

--- lib/wchar.in.h.orig	Thu Jul 29 13:21:38 2010
+++ lib/wchar.in.h
@@ -71,7 +71,20 @@
 #endif
 
 #undef _GL_ALREADY_INCLUDING_WCHAR_H
+#ifdef __minix
+#undef mbsinit
+#define mbsinit rpl_mbsinit
+#undef mbrtowc
+#define mbrtowc rpl_mbrtowc
+#undef wcrtomb
+#define wcrtomb rpl_wcrtomb
+#undef towlower
+#define towlower rpl_towlower
+#undef towupper
+#define towupper rpl_towupper
+#endif /* __minix */
 
+
 #ifndef _GL_WCHAR_H
 #define _GL_WCHAR_H
 
@@ -155,7 +168,6 @@ _GL_WARN_ON_USE (wctob, "wctob is unportable - "
                  "use gnulib module wctob for portability");
 # endif
 #endif
-
 
 /* Test whether *PS is in the initial state.  */
 #if @GNULIB_MBSINIT@
