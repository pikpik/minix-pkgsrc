$NetBSD$

--- lib/wchar.in.h.orig	Tue Mar  1 16:39:30 2011
+++ lib/wchar.in.h
@@ -76,7 +76,20 @@
 #endif
 
 #undef _GL_ALREADY_INCLUDING_WCHAR_H
+#ifdef __minix
+#undef mbsinit
+#define mbsinit rpl_mbsinit
+#undef mbrtowc
+#define mbrtowc rpl_mbrtowc
+#undef wcrtomb
+#define wcrtomb rpl_wcrtomb
+#undef towlower
+#define towlower rpl_towlower
+#undef towupper
+#define towupper rpl_towupper
+#endif /* __minix */
 
+
 #ifndef _GL_WCHAR_H
 #define _GL_WCHAR_H
 
@@ -162,7 +175,6 @@ _GL_WARN_ON_USE (wctob, "wctob is unportable - "
                  "use gnulib module wctob for portability");
 # endif
 #endif
-
 
 /* Test whether *PS is in the initial state.  */
 #if @GNULIB_MBSINIT@
