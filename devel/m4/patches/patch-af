$NetBSD$

--- lib/quotearg.c.orig	Tue Mar  1 16:39:29 2011
+++ lib/quotearg.c
@@ -30,7 +30,6 @@
 #include <stdlib.h>
 #include <string.h>
 #include <wchar.h>
-#include <wctype.h>
 
 #include "gettext.h"
 #define _(msgid) gettext (msgid)
@@ -537,7 +536,11 @@ quotearg_buffer_restyled (char *buffer, size_t buffers
                                 }
                           }
 
+#ifndef __minix
                         if (! iswprint (w))
+#else
+                        if (! isprint (w))
+#endif
                           printable = false;
                         m += bytes;
                       }
