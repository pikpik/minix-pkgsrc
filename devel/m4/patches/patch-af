$NetBSD$

--- lib/quotearg.c.orig	Mon Mar  8 21:47:53 2010
+++ lib/quotearg.c
@@ -31,7 +31,6 @@
 #include <stdlib.h>
 #include <string.h>
 #include <wchar.h>
-#include <wctype.h>
 
 #include "gettext.h"
 #define _(msgid) gettext (msgid)
@@ -538,7 +537,11 @@ quotearg_buffer_restyled (char *buffer, size_t buffers
                                 }
                           }
 
+#ifndef __minix
                         if (! iswprint (w))
+#else
+                        if (! isprint (w))
+#endif
                           printable = false;
                         m += bytes;
                       }
