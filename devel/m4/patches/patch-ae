$NetBSD$

--- lib/fseeko.c.orig	Wed Jul 21 00:13:45 2010
+++ lib/fseeko.c
@@ -31,6 +31,7 @@
 # define fseeko fseek
 #endif
 
+#if 0
 int
 rpl_fseeko (FILE *fp, off_t offset, int whence)
 {
@@ -145,3 +146,10 @@ rpl_fseeko (FILE *fp, off_t offset, int whence)
     }
   return fseeko (fp, offset, whence);
 }
+#else
+int
+rpl_fseeko (FILE *fp, off_t offset, int whence)
+{
+	return fseek(fp, offset, whence);
+}
+#endif
