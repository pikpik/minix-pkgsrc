$NetBSD$

--- gettext-tools/libgrep/m-common.h.orig	Fri Jul  9 12:23:07 2010
+++ gettext-tools/libgrep/m-common.h
@@ -27,6 +27,11 @@
 # include <wctype.h>
 #endif
 
+/* Locale on Minix is broken */
+#ifdef __minix
+#undef MBS_SUPPORT
+#endif
+
 #define NCHAR (UCHAR_MAX + 1)
 
 struct compiled_kwset {
